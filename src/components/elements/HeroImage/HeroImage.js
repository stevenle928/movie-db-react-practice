import React from 'react';
import './HeroImage.css';

const HeroImage = (props) => { //This will be accepting props
    return (
        <div>
            HeroImage
        </div>
    )
}

export default HeroImage;